import styled from 'styled-components';

export const EmptyListInfo = styled.p`
  color: ${({ theme }) => theme.colorText1};
  font-size: 18px;
  margin: 0px;
  line-height: 32px;
  text-align: center;
`;
